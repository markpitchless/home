Custom filetype settings go here. The file should be named <filetype>.vim e.g.
use perl.vim for perl settings.

NB: The global plugin ($VIMRUNTIME/ftplugin/*.vim) will override any settings
here. To avoid this use ~/.vim/after/ftplugin/*.vim
